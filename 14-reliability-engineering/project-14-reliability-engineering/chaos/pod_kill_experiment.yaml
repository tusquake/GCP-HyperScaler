apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: payment-pod-kill-test
  namespace: default
spec:
  action: pod-kill
  mode: one
  duration: "5m"
  selector:
    namespaces: ["default"]
    labelSelectors:
      "app": "node-api"
